<%= form_for(@company) do |f| %>
  <% if @company.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@company.errors.count, "error") %> prohibited this company from being saved:</h2>

      <ul>
      <% @company.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :industry %><br>
    <%= f.text_area :industry %>
  </div>
  <div class="field">
    <%= f.label :turn_over %><br>
    <%= f.number_field :turn_over %>
  </div>
  <div class="field">
    <%= f.label :established_date %><br>
    <%= f.date_select :established_date %>
  </div>
  <div class="field">
    <%= f.label :type_of_company %><br>
    <%= f.text_field :type_of_company %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
